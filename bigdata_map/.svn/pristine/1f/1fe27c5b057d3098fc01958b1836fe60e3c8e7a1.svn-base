package com.edgedo.JT809.client;

public class BPWarningMessage extends BPBaseMessage
{
  public BPWarningMessage(String vehid, short color, short type, byte[] data)
  {
    super(vehid, color, type, data);
  }

  public BPWarningMessage(byte[] buf) throws Exception {
    super(buf);
  }
}